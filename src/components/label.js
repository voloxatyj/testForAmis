import styled from 'styled-components';

export const Label = styled.label`
  color: rgba(0, 0, 0, 0.8);
  font-weight: 600;
  font-size: 12px;
  padding: 0 15px;

  &.label {
    position: absolute;
    bottom: 4vh;
  }
`;
